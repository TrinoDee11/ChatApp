
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= typeof title !== 'undefined' ? title : 'Chat App' %></title>
 

  <!-- Bootstrap CSS -->
<link
  href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
  rel="stylesheet"
  integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
  crossorigin="anonymous"
/>


</head>
<body>
  <header class="site-header">
<div class="container-fluid py-3 px-4 bg-white border-bottom">
  <div class="d-flex justify-content-between align-items-center">
    <a class="text-decoration-none" href="/chat">
      <span class="fw-bold fs-4" style="color: #212529;">Chat</span>
      <span class="fw-bold fs-4" style="color: #34C759;">Lite</span>
    </a>
    
    <nav class="d-flex align-items-center gap-2">
      <% if (typeof currentUser !== 'undefined' && currentUser) { %>
        <span class="me-3 text-muted" style="font-size: 0.9375rem;">
          Hello, <strong class="text-dark"><%= currentUser.name %></strong>
        </span>
        <a class="btn rounded-3 px-3 py-1" 
           href="/logout" 
           style="font-size: 0.9375rem; background-color: #34C759; color: white; border: none;">
          Logout
        </a>
      <% } else { %>
        <a class="btn rounded-3 px-3 py-1" 
           href="/login" 
           style="font-size: 0.9375rem; background-color: #34C759; color: white; border: none;">
          Login
        </a>
        <a class="btn rounded-3 px-3 py-1" 
           href="/register" 
           style="font-size: 0.9375rem; background-color: white; color: #34C759; border: 1px solid #34C759;">
          Sign up
        </a>
      <% } %>
    </nav>
  </div>
</div>
  </header>
  <main class="container">
